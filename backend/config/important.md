for use [server_host] use in [userController] in the [backend_folder]
function of [resetPassword] is 
[${req.protocol}://${req.get("host")}]

-------------- for localhost now i use in config file a variable name like [${FORNTEND_URL}] --------------